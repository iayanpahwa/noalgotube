services:
  noalgotube:
    build: .
    image: iayanpahwa/noalgotube:latest
    ports:
      - "8080:8080"
    volumes:
      - noalgotube_data:/data
    restart: unless-stopped

volumes:
  noalgotube_data:
